TZ: Etc/UTC

resources:
  limits:
    cpus: 2.0
    memory: 512

network:
  networks: []

ix_volumes:
  config: /opt/tests/mnt/config

storage:
  config:
    type: ix_volume
    ix_volume_config:
      dataset_name: config
      create_host_path: true
  additional_storage: []

labels: []
